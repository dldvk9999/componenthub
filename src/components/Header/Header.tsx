import "./Header.scss";

export default function Header() {
    return (
        <header className="Header">
            <a href="/">Home</a>
            <a href="/login">Login</a>
        </header>
    );
}
